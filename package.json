{
  "name": "@voxpelli/node-cli-template",
  "version": "0.0.0",
  "description": "A GitHub template repo for node cli tools",
  "homepage": "http://github.com/voxpelli/node-cli-template",
  "repository": {
    "type": "git",
    "url": "git://github.com/voxpelli/node-cli-template.git"
  },
  "keywords": [],
  "author": "Pelle Wessman <pelle@kodfabrik.se> (http://kodfabrik.se/)",
  "license": "MIT",
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24",
    "typescript": ">=5.8"
  },
  "bin": {
    "node-cli-template": "cli.js"
  },
  "type": "module",
  "files": [
    "cli.js",
    "lib/**/*.js"
  ],
  "scripts": {
    "check-type-tests": "tstyche --target \"5.9\" --reporters list,summary",
    "check:installed-check": "installed-check -i tstyche",
    "check:knip": "knip",
    "check:lint": "eslint",
    "check:tsc": "tsc",
    "check:type-coverage": "type-coverage --detail --strict --at-least 99 --ignore-files 'test/*'",
    "check": "run-s clean && run-p check:* --continue-on-error && run-s check-type-tests",
    "clean:declarations-top": "rm -f $(find . -maxdepth 1 -type f -name '*.d.ts*')",
    "clean:declarations-lib": "rm -f $(find lib -type f -name '*.d.ts*' 2>/dev/null || true)",
    "clean": "run-p clean:*",
    "prepare": "husky",
    "test:node": "c8 --reporter=lcov --reporter=text node --test",
    "test-ci": "run-s test:*",
    "test": "run-s check test:*"
  },
  "dependencies": {
    "@voxpelli/typed-utils": "^1.10.2",
    "markdown-or-chalk": "^0.2.3",
    "peowly": "^1.3.2",
    "pony-cause": "^2.1.11"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "@voxpelli/eslint-config": "^23.0.0",
    "@voxpelli/tsconfig": "^16.1.0",
    "c8": "^11.0.0",
    "eslint": "^9.39.4",
    "husky": "^9.1.7",
    "installed-check": "^10.0.1",
    "knip": "^5.86.0",
    "npm-run-all2": "^8.0.4",
    "tstyche": "^6.2.0",
    "type-coverage": "^2.29.7",
    "typescript": "~5.9.3"
  }
}
